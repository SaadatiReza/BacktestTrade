# Changelog

## v0.1.0 — قدم اول: بک‌تستر SP2L

اولین نسخه‌ی کاری: بک‌اند Python (FastAPI + SQLite) برای گرفتن دیتای قیمت و بک‌تست استراتژی SP2L، به‌همراه فرانت‌اند React برای نمایش نمودار و نتایج.

### استراتژی (SP2L)
- پیاده‌سازی پایپ‌لاین Context → Level → Spike → 2L (HL/LH) → Entry → SL → TP.
- تشخیص Spike بر اساس نسبت بدنه به رنج و اندازه‌ی حرکت نسبت به ATR.
- تشخیص ساختار ۲-لگی (Higher Low در روند صعودی / Lower High در روند نزولی) با قابلیت تنظیم تعداد سوئینگ‌های لازم.
- فیلتر Context (روند تایم‌فریم بالاتر) با EMA.
- دو حالت ورود قابل انتخاب: Retest سطح یا Breakout ساختار.
- SL پشت مبدأ Spike؛ TP به‌صورت تک‌هدفی یا دو-مرحله‌ای (TP1=1R با انتقال استاپ به سر-به-سر، TP2=R:R دلخواه).
- **عمداً پیاده نشده** (چون تعریف دقیقشون فقط در ویدیوی اصلی SP2L هست، نه منابع عمومی): ۴ نوع Spike، P-Gap، مدل ورود 2X.

### بک‌تست و دیتا
- اتصال به Twelve Data برای گرفتن OHLC (نه اسکرپینگ TradingView).
- کش کردن کندل‌ها در SQLite برای جلوگیری از درخواست تکراری.
- صفحه‌بندی خودکار درخواست‌ها برای بازه‌های طولانی/تایم‌فریم پایین (سقف ۵۰۰۰ کندل هر درخواست در Twelve Data).
- موتور شبیه‌سازی معاملات با آمار: تعداد معاملات، Winrate، Avg R، Profit Factor، Max Drawdown.
- محدودیت «یک معامله باز در هر لحظه» با قابلیت خاموش‌کردن (`allow_concurrent_trades`) برای مقایسه‌ی خالص تنظیمات مختلف R:R.

### فرانت‌اند
- نمودار کندل با Lightweight Charts (کتابخونه‌ی رسمی و متن‌باز TradingView).
- انتخاب نماد، تایم‌فریم (از ۱ دقیقه تا ۱ روزه)، بازه‌ی زمانی، و تنظیمات پیشرفته‌ی استراتژی.
- جدول گزارش معاملات با تاریخ/ساعت ورود و خروج، قیمت‌ها، نتیجه‌ی R، و سود/زیان به دلار.
- ماشین‌حساب سود/زیان: با وارد کردن مبلغ ریسک هر معامله، سود کل و سود هر معامله به دلار محاسبه می‌شه.
- کلیک روی هر معامله در جدول: چارت فقط همون معامله رو نمایش می‌ده و روی بازه‌ی زمانی‌ش زوم می‌کنه (برای وضوح بیشتر)؛ کلیک دوباره برمی‌گردونه به نمای کامل.

### شناخته‌شده / نکات باز
- عمق تاریخی دیتا به محدودیت پلن Twelve Data (رایگان/پولی) وابسته‌ست.
- زیرساخت دیتابیس برای توسعه‌ی آینده (سیگنال زنده + بررسی Winrate تجمیعی) از الان با فیلد `Trade.source` آماده شده، ولی خودِ سرویس سیگنال زنده هنوز ساخته نشده.
